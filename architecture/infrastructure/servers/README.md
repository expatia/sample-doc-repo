# Server Infrastructure

Where necessary and where functionality cannot be implemented in serverless infrastructure, containers containing server instances are deployed into Amazon Elastic Container Service (ECS).